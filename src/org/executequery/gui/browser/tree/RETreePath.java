package org.executequery.gui.browser.tree;

import javax.swing.tree.TreePath;

public class RETreePath extends TreePath {
    public RETreePath(TreePath parentPath, Object lastPathComponent) {
        super(parentPath, lastPathComponent);
    }
}
